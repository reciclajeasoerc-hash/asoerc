{
  "name": "asoerc",
  "version": "1.0.0",
  "description": "Sistema de gestión ASOERC ESP",
  "engines": { "node": ">=18" },
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --include=dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/src/app.js"
  }
}
